apiVersion: apps/v1
kind: Deployment
metadata:
  name: learning-lab-api-canary
spec:
  replicas: 1
  selector:
    matchLabels:
      app: learning-lab-api
      track: canary
  template:
    metadata:
      labels:
        app: learning-lab-api
        track: canary
    spec:
      imagePullSecrets:
        - name: ghcr-pull
      containers:
        - name: api
          image: ghcr.io/jesal-tailor/learning-lab-api:sha-d29816e49d352a0f59b3d625d78bacd9c433e801
          imagePullPolicy: Always
          env:
            - name: MODEL_VERSION
              value: "1.0.1"
          ports:
            - containerPort: 8000
